name: Keep Alfa warm

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping production health endpoint
        run: |
          curl --fail --silent --show-error --max-time 20 https://alfa-w3ll.onrender.com/healthz
